import json
 
# Opening JSON file
f = open('benchmark.json')
 
# returns JSON object as
# a dictionary
data = json.load(f)
queries = [['tracks_media_some', 500], ['albums_tracks_genre_some', 500], ['albums_tracks_genre_all', 200]]


def 
for i in range(1,6):
    print(timeparse(data['tracks_media_some on poggers'][str(i * 500)] ))

